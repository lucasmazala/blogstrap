<h1>Articles</h1>

<%= link_to 'New Article', new_article_path%>

<ul>
    <% @articles.each do |article| %>
        <li>
           <!--  <a href="<%= article_path(article) %>">  # Trocado por link_to
                article_path = faz um path para pegarmos um determinado article pelo ID. Não precisa colocar ponto ID porque esse helper sabe automaticamente Aula5
                </a>-->
                <%= link_to article.title, article %>  <!-- primeiro arg é qual string aparecerá para ter o link. Segundo arg(article) é de onde será tirado o ID-->            
        </li>
    <% end %>
</ul>


<button type="button" class="btn btn-secondary" data-bs-toggle="tooltip" data-bs-placement="top" title="Tooltip on top"> <!-- ver problema do tooltip-->
  Tooltip on top
</button>
<button type="button" class="btn btn-secondary" data-bs-toggle="tooltip" data-bs-placement="right" title="Tooltip on right">
  Tooltip on right
</button>
<button type="button" class="btn btn-secondary" data-bs-toggle="tooltip" data-bs-placement="bottom" title="Tooltip on bottom">
  Tooltip on bottom
</button>
<button type="button" class="btn btn-secondary" data-bs-toggle="tooltip" data-bs-placement="left" title="Tooltip on left">
  Tooltip on left
</button>
